# Servidor
server.port=${PORT:8080}

# Datos de Conexión a MySQL (clever cloud por defecto, configurable por variables de entorno)
spring.datasource.url=jdbc:mysql://${DB_HOST:bpljmrsrmouuqribgl2y-mysql.services.clever-cloud.com}:${DB_PORT:3306}/${DB_NAME:bpljmrsrmouuqribgl2y}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USER:ujo14nky1vryxxwz}
spring.datasource.password=${DB_PASSWORD:4JKtKnBjtxdor5SkJi7X}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Configuración Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# Correo para recuperación de clave
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:test@reservahotel.com}
spring.mail.password=${MAIL_PASSWORD:secret}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
